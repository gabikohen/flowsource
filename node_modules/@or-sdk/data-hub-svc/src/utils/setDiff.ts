import _ from 'lodash';

function setDiff<T>(item: T, diff: { [key: string]: unknown; } = {}): T {
  item = _.cloneDeep(item);
  for (const key in diff) _.set(item as {}, key, diff[key]);
  return item;
}

export default setDiff;
