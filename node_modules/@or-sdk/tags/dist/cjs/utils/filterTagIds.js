"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function filterTagIds(tagCollection, tagIdsToFilter) {
    if (tagCollection === void 0) { tagCollection = []; }
    var newIds = [];
    var existingIds = [];
    if (!tagCollection.length) {
        newIds = tagIdsToFilter;
    }
    else {
        tagIdsToFilter.forEach(function (tagId) {
            if (tagCollection.indexOf(tagId) !== -1) {
                existingIds.push(tagId);
            }
            else {
                newIds.push(tagId);
            }
        });
    }
    return {
        newIds: newIds,
        existingIds: existingIds,
    };
}
exports.default = filterTagIds;
//# sourceMappingURL=filterTagIds.js.map